(function(root){
  'use strict';
  const patches = [["#race-hud { position: fixed; inset: 0; z-index: 10; pointer-events: none; }\n.hud-panel { position: absolute; top: 18px; width: 250px; border: 1px solid rgba(255,255,255,.14); border-radius: 16px; padding: 14px 16px; background: rgba(8,11,18,.68); backdrop-filter: blur(12px); box-shadow: 0 14px 35px rgba(0,0,0,.25); }\n.hud-p1 { left: 18px; }\n.hud-p2 { right: 18px; }","#race-hud { position: fixed; inset: 0; z-index: 10; pointer-events: none; }\n.race-roadmap { position: absolute; top: 18px; left: 18px; width: 194px; height: 194px; overflow: hidden; border: 1px solid rgba(255,255,255,.18); border-radius: 18px; background: rgba(8,11,18,.76); backdrop-filter: blur(12px); box-shadow: 0 14px 35px rgba(0,0,0,.28), inset 0 0 28px rgba(81,216,255,.035); }\n.roadmap-head { position: absolute; z-index: 2; top: 10px; left: 12px; right: 12px; display: flex; align-items: center; justify-content: space-between; gap: 8px; pointer-events: none; }\n.roadmap-head span { color: #8ef5ff; font-size: 8px; font-weight: 950; letter-spacing: .15em; }\n.roadmap-head strong { max-width: 116px; overflow: hidden; white-space: nowrap; text-overflow: ellipsis; color: rgba(255,255,255,.72); text-align: right; font-size: 8px; }\n#race-roadmap-canvas { width: 194px; height: 194px; display: block; }\n.roadmap-legend { position: absolute; z-index: 2; left: 12px; right: 12px; bottom: 9px; display: flex; gap: 10px; color: rgba(255,255,255,.58); font-size: 7px; font-weight: 850; pointer-events: none; }\n.roadmap-legend i { display: inline-block; width: 7px; height: 7px; margin-right: 4px; border-radius: 50%; vertical-align: -1px; box-shadow: 0 0 7px currentColor; }\n.roadmap-legend .p1 i { background: #ff4f6d; color: #ff4f6d; }\n.roadmap-legend .p2 i { background: #55d9ff; color: #55d9ff; }\n.hud-panel { position: absolute; top: 18px; width: 250px; border: 1px solid rgba(255,255,255,.14); border-radius: 16px; padding: 14px 16px; background: rgba(8,11,18,.68); backdrop-filter: blur(12px); box-shadow: 0 14px 35px rgba(0,0,0,.25); }\n.hud-p1 { left: 18px; top: 228px; }\n.hud-p2 { right: 18px; }"],["        <div id=\"race-hud\">\n          <div id=\"hud-p1\" class=\"hud-panel hud-p1\">","        <div id=\"race-hud\">\n          <div id=\"race-roadmap\" class=\"race-roadmap\" aria-label=\"현재 코스 로드맵\">\n            <div class=\"roadmap-head\"><span>ROAD MAP</span><strong id=\"roadmap-track-name\">TRACK</strong></div>\n            <canvas id=\"race-roadmap-canvas\" width=\"194\" height=\"194\"></canvas>\n            <div class=\"roadmap-legend\"><span class=\"p1\"><i></i>P1</span><span id=\"roadmap-p2-legend\" class=\"p2\" hidden><i></i>P2</span></div>\n          </div>\n          <div id=\"hud-p1\" class=\"hud-panel hud-p1\">"],["    let lastHudPaint = 0;\n    let tuningCarId = CarCatalog.CARS[0].id;","    let lastHudPaint = 0;\n    let tuningCarId = CarCatalog.CARS[0].id;\n    let roadmapCache = { key: '', route: null, samples: [], projected: [] };"],["    function updateHud(snapshot, force = false) {\n","    function getRoadmapModel() {\n      const route = TrackCatalog.getTrack(setup.trackId, setup.distance);\n      if (!route) return null;\n      const key = `${route.themeId}:${route.distance}`;\n      const canvasElement = byId('race-roadmap-canvas');\n      if (!canvasElement) return null;\n      if (roadmapCache.key === key && roadmapCache.projected.length) return roadmapCache;\n\n      const sampled = TrackBuilder.sampleClosedSpline(route.points, 260).samples;\n      const xs = sampled.map((sample) => sample.position[0]);\n      const zs = sampled.map((sample) => sample.position[2]);\n      const minX = Math.min(...xs), maxX = Math.max(...xs);\n      const minZ = Math.min(...zs), maxZ = Math.max(...zs);\n      const spanX = Math.max(1, maxX - minX);\n      const spanZ = Math.max(1, maxZ - minZ);\n      const padding = 25;\n      const drawableW = canvasElement.width - padding * 2;\n      const drawableH = canvasElement.height - padding * 2;\n      const scale = Math.min(drawableW / spanX, drawableH / spanZ);\n      const usedW = spanX * scale;\n      const usedH = spanZ * scale;\n      const offsetX = (canvasElement.width - usedW) * 0.5;\n      const offsetY = (canvasElement.height - usedH) * 0.5;\n      const projected = sampled.map((sample) => ({\n        x: offsetX + (sample.position[0] - minX) * scale,\n        y: offsetY + (sample.position[2] - minZ) * scale,\n      }));\n      roadmapCache = { key, route, samples: sampled, projected };\n      return roadmapCache;\n    }\n\n    function drawStartFinishMarker(ctx, model) {\n      if (!model?.projected?.length) return;\n      const a = model.projected[0];\n      const b = model.projected[3 % model.projected.length];\n      const dx = b.x - a.x;\n      const dy = b.y - a.y;\n      const length = Math.max(1, Math.hypot(dx, dy));\n      const nx = -dy / length;\n      const ny = dx / length;\n      ctx.save();\n      ctx.lineWidth = 4;\n      ctx.strokeStyle = '#ffffff';\n      ctx.beginPath();\n      ctx.moveTo(a.x - nx * 7, a.y - ny * 7);\n      ctx.lineTo(a.x + nx * 7, a.y + ny * 7);\n      ctx.stroke();\n      ctx.lineWidth = 2;\n      ctx.strokeStyle = '#111827';\n      ctx.setLineDash([3, 3]);\n      ctx.stroke();\n      ctx.restore();\n    }\n\n    function drawRoadmap(snapshot) {\n      const canvasElement = byId('race-roadmap-canvas');\n      if (!canvasElement || !snapshot?.hud) return;\n      const model = getRoadmapModel();\n      if (!model?.projected?.length) return;\n      const ctx = canvasElement.getContext('2d');\n      if (!ctx) return;\n      const width = canvasElement.width;\n      const height = canvasElement.height;\n      ctx.clearRect(0, 0, width, height);\n\n      const glow = ctx.createRadialGradient(width * 0.48, height * 0.48, 10, width * 0.48, height * 0.48, width * 0.7);\n      glow.addColorStop(0, 'rgba(31,55,78,.22)');\n      glow.addColorStop(1, 'rgba(4,7,12,.04)');\n      ctx.fillStyle = glow;\n      ctx.fillRect(0, 0, width, height);\n\n      const tracePath = () => {\n        ctx.beginPath();\n        ctx.moveTo(model.projected[0].x, model.projected[0].y);\n        for (let index = 1; index < model.projected.length; index += 1) {\n          ctx.lineTo(model.projected[index].x, model.projected[index].y);\n        }\n        ctx.closePath();\n      };\n\n      ctx.save();\n      ctx.lineJoin = 'round';\n      ctx.lineCap = 'round';\n      tracePath();\n      ctx.strokeStyle = 'rgba(0,0,0,.62)';\n      ctx.lineWidth = 12;\n      ctx.stroke();\n      tracePath();\n      ctx.strokeStyle = 'rgba(224,235,247,.42)';\n      ctx.lineWidth = 7;\n      ctx.stroke();\n      tracePath();\n      ctx.strokeStyle = 'rgba(255,255,255,.92)';\n      ctx.lineWidth = 2.1;\n      ctx.stroke();\n      ctx.restore();\n\n      drawStartFinishMarker(ctx, model);\n\n      const playerColors = ['#ff4f6d', '#55d9ff'];\n      snapshot.hud.forEach((hud, index) => {\n        if (!hud || index > 1) return;\n        const count = model.projected.length;\n        const normalized = ((Number(hud.progress) || 0) % 1 + 1) % 1;\n        const exact = normalized * count;\n        const currentIndex = Math.floor(exact) % count;\n        const nextIndex = (currentIndex + 1) % count;\n        const t = exact - Math.floor(exact);\n        const current = model.projected[currentIndex];\n        const next = model.projected[nextIndex];\n        const x = current.x + (next.x - current.x) * t;\n        const y = current.y + (next.y - current.y) * t;\n        const heading = Math.atan2(next.y - current.y, next.x - current.x);\n        const color = playerColors[index];\n\n        ctx.save();\n        ctx.translate(x, y);\n        ctx.rotate(heading);\n        ctx.shadowColor = color;\n        ctx.shadowBlur = 11;\n        ctx.fillStyle = color;\n        ctx.strokeStyle = '#071018';\n        ctx.lineWidth = 2;\n        ctx.beginPath();\n        ctx.moveTo(8, 0);\n        ctx.lineTo(-5.5, -5);\n        ctx.lineTo(-3.4, 0);\n        ctx.lineTo(-5.5, 5);\n        ctx.closePath();\n        ctx.fill();\n        ctx.stroke();\n        ctx.restore();\n\n        ctx.save();\n        ctx.font = '950 8px system-ui, sans-serif';\n        ctx.textAlign = 'center';\n        ctx.fillStyle = '#ffffff';\n        ctx.shadowColor = '#000000';\n        ctx.shadowBlur = 4;\n        ctx.fillText(`P${index + 1}`, x, y - 10);\n        ctx.restore();\n      });\n\n      byId('roadmap-track-name').textContent = model.route.name;\n      byId('roadmap-p2-legend').hidden = snapshot.mode !== '2p';\n    }\n\n    function updateHud(snapshot, force = false) {\n"],["      byId('race-timer').textContent = formatTime(snapshot.raceTime);\n","      drawRoadmap(snapshot);\n      byId('race-timer').textContent = formatTime(snapshot.raceTime);\n"]];
  root.applySeojunRoadmapPatch = function applySeojunRoadmapPatch(sourceHtml) {
    let html = String(sourceHtml || '');
    for (const [before, after] of patches) {
      if (!html.includes(before)) throw new Error('로드맵 패치 기준점을 찾지 못했습니다.');
      html = html.replace(before, after);
    }
    return html;
  };
})(typeof globalThis !== 'undefined' ? globalThis : window);
